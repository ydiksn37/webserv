# SCALE FOR PROJECT WEBSERV

**評価対象:** (学生名)
**Gitリポジトリ:** (リポジトリURL)

## はじめに (Introduction)

以下のルールを遵守してください：
* 評価プロセス全体を通して、礼儀正しく、丁寧で、敬意を払い、建設的な態度を保ってください。コミュニティの健全性はそれに依存しています。
* 評価対象者（またはグループ）と一緒に、作品に機能不全がある場合はそれを特定してください。特定した問題について議論し合う時間を設けてください。
* プロジェクトの指示や機能の範囲について、他の学生（ピア）がどのように理解したかには違いがある可能性があることを考慮しなければなりません。常にオープンマインドを保ち、可能な限り誠実に評価してください。ピア評価が真剣に行われた場合にのみ、この教育法は有効です。

## ガイドライン (Guidelines)

* 学生またはグループのGitリポジトリにある作品のみを評価してください。
* Gitリポジトリがその学生またはグループのものであることを再確認してください。プロジェクトに関連する作品であることを確認し、空のフォルダで `git clone` が使用されていることも確認してください。
* 悪意のあるエイリアスが使用されて、公式リポジトリの内容以外のものを評価させようとしていないか、注意深く確認してください。
* この評価スケールによって要求されるテストを容易にするために、学生が提供するスクリプトを注意深く検証してください。
* プロジェクトの一部も完了していない場合（必須パートなど）、評価を直ちに中止し、評価を終了してください。
* 評価基準が満たされていない場合、関連するセクションは「No（いいえ）」と評価され、それ以上の評価は行われません。ただし、評価を続行して、不足している点や次の機会に向けて改善すべき点についてアドバイスを提供することはできます。
* 「Yes（はい）」で検証される項目は、完全に機能している必要があります。いかなるエラー（セグメンテーションフォールトなど）であっても、エラーが発生した場合は、その項目全体を失敗として扱う必要があります。
* このスケールでは、CGIの実装にPHPとPythonを使用することを前提としていますが、他の言語（C、C++など）でも動作するはずです。

---

## 必須パート (Mandatory Part)

### 準備 (Preparation)

* 設定ファイルが期待通りに機能することを確認してください。
* プロジェクトを実行するための実行可能ファイル（webserv）を提供する `Makefile` があるか確認してください。
* プロジェクトを起動します。起動しない場合は、評価を停止してください。

> 評価： Yes / No

---

### ポートとデフォルトファイル (Port and Default File)

設定ファイルで、ポート番号が適切に設定されていること、および「デフォルトの」ファイルが存在するか（指定されたディレクトリでの `index.html` 検索やディレクトリのインデックス表示など）を確認してください。

* ブラウザを開き、`localhost:ポート番号/` にアクセスします。
* エラーがなく、正しい応答が返ってくるか確認してください。
* エラーがある場合は、なぜエラーが発生したのかを学生に説明してもらってください（たとえば、ファイルが存在しない、権限が拒否されているなど）。エラーが正当化できない場合は、評価を停止してください。

> 評価： Yes / No

---

### メソッド (Methods)

設定ファイルで設定されたポートでテストします。

#### GET メソッド
* ブラウザとcurlコマンドの両方を使って、通常の `GET` リクエストが機能するか確認してください。

> 評価： Yes / No

#### POST メソッド
* `POST` を使用してファイルをアップロードできるか、curlコマンドやファイル転送のスクリプト等を使って確認してください。

> 評価： Yes / No

#### DELETE メソッド
* 指定したファイルが削除できるか確認してください。削除したファイルを再び `GET` した際に、正しく「見つからない」エラー（404 Not Found など）が返ってくるか確認してください。
* 削除の権限を持たないファイルに対してリクエストを送り、正しいエラーが返ってくるか確認してください。

> 評価： Yes / No

#### 不明なメソッド (Unknown Method)
* サーバーが処理できないメソッド（例：`OPTIONS`、`PUT` など、設定されていないもの）を送信し、サーバーがクラッシュせずに正しいエラー応答を返すか確認してください。

> 評価： Yes / No

---

### エラーページ (Error Pages)

* ブラウザで、存在しないページにアクセスしてください。
* プロジェクトでデフォルトのエラーページが定義されていることを確認してください。
* 設定ファイルでエラーページがカスタマイズ可能であるか確認してください（例：404用の特定のページを設定する）。

> 評価： Yes / No

---

### CGI の実行 (CGI Execution)

設定ファイルで設定されたポートでテストします。

#### ファイル拡張子に基づく実行
* CGI（Python、PHP など）を実行してください。
* 要求されたファイル拡張子（`.php`, `.py` など）に応じてCGIが正しく呼び出されているか確認してください。
* もし拡張子が一致しない場合は、CGIが実行されないことを確認してください。

> 評価： Yes / No

#### POST メソッドとの連携
* フォームを持つページからデータを送信（`POST`）し、CGIがそのデータを正しく受け取って処理できるか確認してください。

> 評価： Yes / No

#### GET メソッドとの連携
* URLのクエリパラメータ（例：`/test.php?name=42`）付きで `GET` リクエストを送信し、CGIがそのパラメータを正しく受け取って処理できるか確認してください。

> 評価： Yes / No

---

### チャンク転送 (Chunked Transfer)

* 設定ファイルの `client_max_body_size` を超えるデータを、チャンク化（`Transfer-Encoding: chunked`）して送信してみてください。サーバーは正しく処理（または設定により拒否）するか確認してください。
* チャンク化されたデータを送信し、データが完全にサーバーで再構築されるか確認してください。
* もしクライアント側の処理や設定が機能していない場合は評価を続けてください。

> 評価： Yes / No

---

### マルチポートとホスト (Multiple Ports and Hosts)

* サーバーが複数のポートで待ち受け可能であることを確認してください。
* ブラウザを使用して各ポートにアクセスし、それぞれの設定で指定された正しいウェブサイトが配信されるか確認してください。
* 同じポートに対して、複数のホスト名（`server_name`）が設定されているか確認してください。異なるホスト名でアクセスした際に、正しいウェブサイトが配信されるか確認してください。

> 評価： Yes / No

---

### Siege & ストレステスト (Siege & Stress Test)

* `siege` ツールを使ってストレステストを実行してください。
* 空のページに対して `siege -b` を実行し、単純なGETリクエストに対する可用性が99.5%以上であることを確認してください。
* メモリリークがないか確認してください（プロセスのメモリ使用量を監視し、無限に増加しないことを確認してください）。
* 接続がハング（応答停止）していないか確認してください。
* サーバーを再起動することなく、`siege` を無期限に使用できるはずです（`siege -b` を参照）。

> 評価： Yes / No

---

## ボーナスパート (Bonus Part)

**重要:** ボーナスパートは、必須パートが問題なく完全に完了した場合にのみ評価されます。必須要件をすべて満たしていない場合、ボーナスパートは評価されません。

### クッキーとセッション (Cookies and Session)

* ウェブサーバー上で機能するセッションとクッキーのシステムがあるか確認してください。

> 評価： Yes / No

### CGI (複数対応)

* 複数のCGIシステム（例：PHPとPythonの両方）がサポートされているか確認してください。

> 評価： Yes / No

---

## 評価結果 (Ratings)

* [ ] Ok
* [ ] Outstanding project (傑出したプロジェクト)
* [ ] Empty work (白紙)
* [ ] No author file (著者ファイルなし)
* [ ] Invalid compilation (コンパイル無効)
* [ ] Norme (Norm違反)
* [ ] Cheat (不正行為)
* [ ] Crash (クラッシュ)
* [ ] Leaks (メモリリーク)
* [ ] Forbidden function (禁止関数の使用)

**Conclusion (結論)**
この評価に関するコメントを残してください。
