# Webserv
**URLがなぜHTTPで始まるのか、ついに理解する時が来ました**

**概要:**
このプロジェクトは、独自のHTTPサーバーを作成するものです。完成したサーバーは、実際のブラウザを使用してテストできるようになります。
HTTPは、インターネット上で最も広く使用されているプロトコルの1つです。Webサイトの仕事に携わらない場合でも、その複雑さを理解することは役立ちます。
**バージョン:** 24.0

## 目次
| 章 | タイトル |
| :--- | :--- |
| I | はじめに (Introduction) |
| II | 一般規則 (General rules) |
| III | AIの指示 (AI Instructions) |
| IV | 必須パート (Mandatory part) |
| IV.1 | 要件 (Requirements) |
| IV.2 | MacOS専用要件 (For MacOS only) |
| IV.3 | 設定ファイル (Configuration file) |
| V | Readme要件 (Readme Requirements) |
| VI | ボーナスパート (Bonus part) |
| VII | 提出とピア評価 (Submission and peer-evaluation) |

---

## 第1章 はじめに
Hypertext Transfer Protocol（HTTP）は、分散型・コラボレーション型・ハイパーメディア情報システムのためのアプリケーション・プロトコルです。
HTTPは、World Wide Webにおけるデータ通信の基盤であり、ハイパーテキスト・ドキュメントには、ユーザーが簡単にアクセスできる他のリソースへのハイパーリンクが含まれています。たとえば、マウスボタンのクリックやWebブラウザの画面のタップなどです。
HTTPは、ハイパーテキスト機能のサポートとWorld Wide Webの成長のために開発されました。Webサーバーの主な機能は、Webページを保存、処理、そしてクライアントに配信することです。クライアントとサーバー間の通信はHTTPを通じて行われます。
配信されるページは最も頻繁にHTMLドキュメントであり、テキストコンテンツに加えて、画像、スタイルシート、スクリプトが含まれる場合があります。トラフィックの多いWebサイトでは、複数の物理マシン間でトラフィックを分割するために、複数のWebサーバーが使用されることがあります。
一般的にWebブラウザやWebクローラーであるユーザーエージェントが、HTTPを使用して特定のリソースを要求することで通信を開始し、サーバーはそのリソースのコンテンツ、またはそれができない場合はエラーメッセージで応答します。リソースは通常、サーバーのストレージ上の実際のファイル、またはプログラムの実行結果です。しかし、常にそうとは限らず、実際には様々なケースがあり得ます。
主な機能はコンテンツの配信ですが、HTTPはクライアントがデータを送信することも可能にします。この機能は、ファイルのアップロードを含む、Webフォームの送信に使用されます。

---

## 第2章 一般規則
* あなたのプログラムは、いかなる状況下（メモリ不足に陥った場合など）であってもクラッシュしたり、予期せぬ終了をしてはなりません。これが発生した場合、プロジェクトは機能しないものとみなされ、評価は0点になります。
* ソースファイルをコンパイルする `Makefile` を提出する必要があります。不必要な再リンク（relinking）を行ってはなりません。
* `Makefile` には少なくとも以下のルールを含める必要があります:
  `$(NAME)`, `all`, `clean`, `fclean`, `re`。
* コードは `c++` とフラグ `-Wall -Wextra -Werror` でコンパイルしてください。
* コードは `C++98` 規格に準拠している必要があり、`-std=c++98` フラグを追加してもコンパイルできなければなりません。
* 可能な限りC++の機能を活用してください（例: `<string.h>` よりも `<cstring>` を選択する）。C言語の関数を使用することは許可されていますが、可能であれば常にC++版を優先して使用してください。
* 外部ライブラリおよびBoostライブラリの使用は禁止されています。

---

## 第3章 AI指示
**コンテキスト**
学習の旅の中で、AIは様々なタスクの支援になります。AIツールの多様な機能と、それがどのようにあなたの作業をサポートできるかを探求する時間を取ってください。しかし、常に注意深くアプローチし、その結果を批判的に評価してください。コード、ドキュメント、アイデア、技術的な説明のいずれであっても、質問が適切に構成されていたか、または生成されたコンテンツが正確であるかを完全に確信することはできません。仲間（ピア）は、間違いや盲点を避けるための貴重なリソースです。

**メインメッセージ**
* AIを使用して、反復的または退屈なタスクを減らしてください。
* プログラミングとそれ以外の両方において、将来のキャリアに役立つプロンプトスキルを開発してください。
* 一般的なリスク、バイアス、倫理的問題をより良く予測し回避するために、AIシステムがどのように機能するかを学んでください。
* 仲間と協力して、技術的スキルとヒューマンスキルの両方を構築し続けてください。
* 自分が完全に理解し、責任を持てるAI生成コンテンツのみを使用してください。

**学習者のルール:**
* AIツールを探求し、それがどのように機能するかを理解する時間を取るべきです。それにより、倫理的に使用し、潜在的なバイアスを減らすことができます。
* プロンプトを出す前に、自分の問題について考えるべきです。これにより、正確な語彙を使用して、より明確で、詳細で、関連性の高いプロンプトを作成することができます。
* AIによって生成されたものを体系的にチェック、レビュー、質問、テストする習慣を身につけるべきです。
* 常にピアレビューを求めるべきです。自分自身の検証だけに頼ってはいけません。

**フェーズの成果:**
* 汎用およびドメイン特化の両方のプロンプトスキルを開発する。
* AIツールの効果的な使用により、生産性を向上させる。
* コンピューテーショナル・シンキング、問題解決能力、適応力、コラボレーション能力の強化を続ける。

**コメントと例:**
* 試験、評価など、真の理解を示さなければならない状況に定期的に遭遇します。準備を怠らず、技術的スキルと対人スキルの両方を構築し続けてください。
* 自分の推論を説明し、仲間と議論することは、理解のギャップを明らかにすることがよくあります。ピア・ラーニングを優先してください。
* AIツールには特定のコンテキストが欠けていることが多く、一般的な回答を提供する傾向があります。環境を共有する仲間は、より関連性が高く正確な洞察を提供できます。
* AIは最も可能性の高い答えを生成する傾向がありますが、仲間は代替の視点や貴重なニュアンスを提供できます。品質のチェックポイントとして彼らを頼りにしてください。

* **良い実践の例:** ソート関数をどうテストすればよいかAIに尋ね、いくつかのアイデアを得る。それを試し、結果を仲間とレビューし、一緒にアプローチを改善する。
* **悪い実践の例:** AIに関数全体を書かせ、プロジェクトにコピペする。ピア評価の際、それが何をするのか、なぜそうするのかを説明できず、信頼を失いプロジェクトに不合格となる。
* **良い実践の例:** パーサーの設計にAIを使用し、その後、ロジックを仲間と確認する。2つのバグを見つけ、より良くクリーンで、完全に理解できるものとして一緒に書き直す。
* **悪い実践の例:** プロジェクトの重要な部分のコードをCopilotに生成させる。コンパイルは通るが、パイプの処理方法を説明できない。評価で正当性を示せず、プロジェクトに不合格となる。

---

## 第4章 必須パート

| 項目 | 内容 |
| :--- | :--- |
| **プログラム名** | `webserv` |
| **提出ファイル** | `Makefile`, `*.{h, hpp}`, `*.cpp`, `*.tpp`, `*.ipp`, 設定ファイル |
| **Makefile** | `NAME`, `all`, `clean`, `fclean`, `re` |
| **引数** | `[設定ファイル]` |
| **許可される外部関数** | すべての機能は `C++ 98` で実装する必要があります。<br>`execve`, `pipe`, `strerror`, `gai_strerror`, `errno`, `dup`, `dup2`, `fork`, `socketpair`, `htons`, `htonl`, `ntohs`, `ntohl`, `select`, `poll`, `epoll` (`epoll_create`, `epoll_ctl`, `epoll_wait`), `kqueue` (`kqueue`, `kevent`), `socket`, `accept`, `listen`, `send`, `recv`, `chdir`, `bind`, `connect`, `getaddrinfo`, `freeaddrinfo`, `setsockopt`, `getsockname`, `getprotobyname`, `fcntl`, `close`, `read`, `write`, `waitpid`, `kill`, `signal`, `access`, `stat`, `open`, `opendir`, `readdir`, `closedir`。 |
| **Libftの使用** | 適用外 (`n/a`) |
| **説明** | `C++ 98` によるHTTPサーバー |

* `C++ 98` でHTTPサーバーを作成する必要があります。
* 実行ファイルは次のように実行される必要があります:
  `./webserv [設定ファイル]`
* （注釈）課題や評価シートで `poll()` が言及されていても、`select()`, `kqueue()`, `epoll()` などの等価な関数を使用することができます。
* プロジェクトを始める前に、HTTPプロトコルを定義するRFCを読み、`telnet` と NGINX を使ってテストを実行してください。RFC全体を実装する必要はありませんが、読むことで必要な機能の開発に役立ちます。参考としてHTTP 1.0を推奨しますが、強制ではありません。

### IV.1 要件
* プログラムは、コマンドライン引数として提供されるか、またはデフォルトパスにある設定ファイルを使用しなければなりません。
* 別のWebサーバーを `execve` することはできません。
* サーバーは常にノンブロッキング状態を維持し、必要に応じてクライアントの切断を適切に処理する必要があります。
* ノンブロッキングであり、クライアントとサーバー間のすべてのI/O操作（`listen` を含む）に対して、単一の `poll()` （または等価な関数）のみを使用する必要があります。
* `poll()` （または等価な関数）は、読み取りと書き込みの両方を同時に監視しなければなりません。
* `poll()` （または等価な関数）を経由せずに読み取りや書き込み操作を行ってはなりません。
* 読み取りまたは書き込み操作の後に、`errno` の値を確認してサーバーの動作を調整することは**厳しく禁止**されています。
* 通常のディスクファイルに対しては `poll()` （または等価な関数）を使用する必要はありません。これらへの `read()` および `write()` はレディネス通知を必要としません。

> **重要 (警告):** データを待つ可能性のあるI/O（ソケット、パイプ/FIFOなど）はノンブロッキングであり、単一の `poll()` （または等価な関数）によって駆動されなければなりません。事前に準備ができているか（readiness）を確認せずに、これらの記述子に対して `read/recv` または `write/send` を呼び出すと、評価は0点になります。通常のディスクファイルは免除されます。

* `poll()` や等価な呼び出しを使用する際、関連するすべてのマクロやヘルパー関数（例: `select()` 用の `FD_SET`）を使用できます。
* サーバーへのリクエストが無限にハングしてはなりません。
* サーバーは、任意の標準的なWebブラウザと互換性がなければなりません。
* NGINXは、ヘッダーと応答の動作を比較するために使用できます（HTTPバージョンの違いに注意してください）。
* HTTP応答ステータスコードは正確である必要があります。
* エラーページが提供されていない場合、サーバーにはデフォルトのエラーページが必要です。
* CGI（PHPやPythonなど）以外の目的で `fork` を使用することはできません。
* 完全に静的なWebサイトを配信できなければなりません。
* クライアントはファイルをアップロードできなければなりません。
* 少なくとも `GET`, `POST`, `DELETE` メソッドが必要です。
* サーバーに対してストレステストを行い、常に利用可能であることを確認してください。
* サーバーは、異なるコンテンツを配信するために複数のポートで待ち受け可能でなければなりません（設定ファイルの項目を参照）。

> **注釈:** 意図的にHTTP RFCのサブセットのみを提供することを選択しました。この文脈では、バーチャルホスト機能はスコープ外と見なされます。しかし、実装したい場合は許可されています。

### IV.2 MacOS専用要件
macOSは他のUnix系OSとは異なる方法で `write()` を処理するため、`fcntl()` の使用が許可されています。
他のUnix OSと同様の動作を実現するために、ファイルディスクリプタをノンブロッキングモードで使用する必要があります。

> **重要:** ただし、`fcntl()` の使用は以下のフラグでのみ許可されています:
> `F_SETFL`, `O_NONBLOCK`, `FD_CLOEXEC`。
> その他のフラグは禁止されています。

### IV.3 設定ファイル
NGINXの設定ファイルの `server` セクションからインスピレーションを得ることができます。
設定ファイルでは、以下のことができる必要があります:
* サーバーが待ち受ける（プログラムが提供する複数のWebサイトを定義する）すべての `interface:port` ペアを定義する。
* デフォルトのエラーページを設定する。
* クライアントリクエストボディの最大許容サイズを設定する。
* Webサイトの特定のURL/ルートに関するルールや設定（ここで正規表現は不要です）を指定する。以下の項目が含まれます:
  * そのルートで許可されるHTTPメソッドのリスト。
  * HTTPリダイレクト。
  * 要求されたファイルが存在すべきディレクトリ（例: URL `/kapouet` が `/tmp/www` にルート付けされている場合、URL `/kapouet/pouic/toto/pouet` は `/tmp/www/pouic/toto/pouet` を検索します）。
  * ディレクトリリスティングの有効化または無効化。
  * リクエストされたリソースがディレクトリである場合に提供するデフォルトファイル。
* クライアントからサーバーへのファイルのアップロードを許可し、保存場所を指定する。
* ファイル拡張子（例: `.php`）に基づくCGIの実行。CGIに関する特記事項:
  * CGIとは何か疑問に思いますか？
  * WebサーバーとCGI間の通信に関与する環境変数を注意深く確認してください。クライアントから提供された完全なリクエストと引数が、CGIで利用可能でなければなりません。
  * チャンク化されたリクエストの場合、サーバーはチャンクを解除（un-chunk）する必要があり、CGIはボディの終わりとして `EOF` を期待することを覚えておいてください。
  * 同じことがCGIの出力にも当てはまります。CGIから `content_length` が返されない場合、`EOF` が返されたデータの終わりを示します。
  * CGIは、相対パスのファイルアクセスのために正しいディレクトリで実行されるべきです。
  * サーバーは少なくとも1つのCGI（php-CGI、Pythonなど）をサポートしている必要があります。

評価中にすべての機能が機能することをテストし実証するために、設定ファイルとデフォルトファイルを提供する必要があります。
ファイル内に他のルールや設定情報（バーチャルホストを実装する予定の場合のWebサイトのサーバー名など）を含めることができます。

* 特定の動作に関する質問がある場合は、プログラムの動作をNGINXと比較できます。
* 小さなテスターが提供されています。ブラウザとテストですべて問題なく機能する場合は、これを使用することは必須ではありませんが、バグを見つけて修正するのに役立ちます。
* レジリエンス（回復力）が鍵です。サーバーは常に動作し続ける必要があります。
* 1つのプログラムだけでテストしないでください。PythonやGolangなどのより適切な言語、お好みであればCやC++でテストを書いてください。

---

## 第5章 Readme要件
Gitリポジトリのルートに `README.md` ファイルを提供する必要があります。この目的は、プロジェクトに馴染みのない人（仲間、スタッフ、採用担当者など）が、プロジェクトの目的、実行方法、トピックに関する詳細情報の場所をすばやく理解できるようにすることです。

`README.md` には少なくとも以下を含める必要があります:
* 最初の行は斜体（イタリック）で、次のように記述する必要があります: *This project has been created as part of the 42 curriculum by <login1>[, <login2>[, <login3>[...]]].*
* 目標と簡単な概要を含む、プロジェクトを明確に提示する「Description（説明）」セクション。
* コンパイル、インストール、および/または実行に関する関連情報を含む「Instructions（手順）」セクション。
* トピックに関連する古典的なリファレンス（ドキュメント、記事、チュートリアルなど）のリストと、AIがどのように使用されたか（どのタスクとプロジェクトのどの部分で指定されたか）の説明を含む「Resources（リソース）」セクション。

プロジェクトによっては、追加のセクション（例: 使用例、機能リスト、技術的な選択など）が必要になる場合があります。必要な追加事項は明示的にリストされます。
READMEは英語で記述する必要があります。

---

## 第6章 ボーナスパート
以下は、実装できる追加機能の例です:
* Cookieとセッション管理のサポート（簡単な例を提供してください）。
* 複数のCGIタイプの処理。

> **重要:** ボーナスパートは、必須パートが問題なく完全に完了した場合にのみ評価されます。必須要件をすべて満たしていない場合、ボーナスパートは評価されません。

---

## 第7章 提出とピア評価
通常通り、Gitリポジトリに課題を提出してください。評価中（defense）は、リポジトリのコンテンツのみが評価されます。ファイル名が正しいことを必ず再確認してください。

評価中、時折プロジェクトの簡単な修正が求められることがあります。これには、動作の小さな変更、数行のコードの記述や書き直し、または簡単に追加できる機能が含まれる可能性があります。
このステップはすべてのプロジェクトに適用されるわけではありませんが、評価ガイドラインで言及されている場合は準備をしておく必要があります。このステップは、プロジェクトの特定の部分に関するあなたの実際の理解を確認することを目的としています。

変更は、選択した任意の開発環境（例: 通常のセットアップ）で実行でき、評価の一環として特定の時間枠が定義されていない限り、数分以内で実行可能であるべきです。
たとえば、関数やスクリプトの小さな更新、表示の変更、または新しい情報を保存するためのデータ構造の調整などを求められる場合があります。詳細（スコープ、ターゲットなど）は評価ガイドラインで指定され、同じプロジェクトでも評価ごとに異なる場合があります。
