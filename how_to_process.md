# Webserv: アーキテクチャ設計と担当分け
本書は、Webservプロジェクトを3人でコンフリクトなく、かつ同時進行で開発するための設計と担当分けを定義する。<br>
CGI実装に入る前に、まずは以下の3つの巨大モジュールを独立して完成させ、結合することを目標とする。

## インターフェースの作成
各担当が独立して作業を始める前に、**モジュール間のデータの受け渡しルール（クラスの型）** を絶対のルールとして定める。<br>
`main` ブランチにて、以下のヘッダーファイルの雛形（中身は空でよい）を作成してから各自のブランチを切ること。

### A. Config → 全体への提供
設定ファイルから読み取った情報を渡すためのクラス。

```cpp
// Config.hpp (雛形イメージ)
class ServerConfig {
private:
	// ...etc

public:
  // ...etc
};
```

### B. Engine ⇔ HTTP 間の連携
Engine（ソケット通信）とHTTP（文字列解析）の境界線となる関数群。

```cpp
// Engine.hpp (雛形イメージ)
class Engine {
private:
	// ...etc

public:
  // ...etc
};
```

### C. Engine（通信・イベントループ）
ソケットの生々しい操作を隠蔽し、イベントの通知に専念します。

```cpp
// EventLoop.hpp (雛形イメージ)
class EventLoop {
private:
	// ...etc

public:
  // ...etc
};
```

---

## 担当分け

上記のインターフェースを基に、以下の3領域に分かれて開発をスタートする。

### 👤 担当A：Config（設定ファイル解析）
**【ミッション】**
`default.conf` などのテキストファイルを読み込み、C++のプログラム内で扱いやすいデータ構造（`ServerConfig` クラスなど）に変換する。

**【主な実装タスク】**
- `std::ifstream` を用いたファイル読み込み。
- 設定ファイルの字句解析（トークナイズ）と `{ }` ブロックの構文解析（パーサー）。
- 必須ディレクティブ（`listen`, `server_name`, `client_max_body_size`, `error_page`, `location`内の各種設定）のパース。
- 構文エラー（セミコロン忘れ、不正な値）のハンドリングとサーバー起動阻止。
- ボーナスに対応する場合、設定ファイルのCGI情報を保存する変数を、単一の文字列ではなく拡張子と実行パスのペア（std::map）にする必要がある。

**【独立テストの方法】**
ネットワーク通信は一切不要。`main` 関数からパーサーを呼び出し、パース結果を `std::cout` で出力して正しく読み取れているか確認する。

### 👤 担当B：HTTP（プロトコル処理・文字列解析）
**【ミッション】**
クライアントから送られてきた生の文字列（リクエスト）を解析し、適切なHTML文字列（レスポンス）を作り出す。

**【主な実装タスク】**
- `GET / HTTP/1.1\r\n...` を `HttpRequest` クラスに変換するパーサー（ヘッダーとボディの分離）。
- `Transfer-Encoding: chunked` の結合処理（Un-chunk）。
- 適切なステータスコード（200, 404, 405, 413 など）の判定。
- エラーページの読み込み、またはディレクトリリスティング（autoindex）用HTMLの動的生成。
- 最終的な送信文字列（`HTTP/1.1 200 OK\r\n...`）の構築。
- ボーナスのクッキーとセッション (Cookies and Session)に対応する場合、リクエストのヘッダーを解析する際、特定のヘッダー（Cookie など）だけを特別扱いせず、<br>
すべてのヘッダーを std::map<std::string, std::string> のような辞書に格納する設計にする必要がある。

**【独立テストの方法】**
ソケット通信は一切不要。ハードコーディングした「擬似的なHTTP文字列」を自作の関数に渡し、期待通りのレスポンス文字列が返ってくるかテストする。

### 👤 担当C：Engine（通信制御・イベントループ）
**【ミッション】**
サーバーの心臓部となる、ノンブロッキング通信と `poll()`（または `kqueue`）による複数クライアントの監視・制御ループを構築する。

**【主な実装タスク】**
- `socket`, `setsockopt` (SO_REUSEADDR必須), `bind`, `listen` によるサーバー立ち上げ。
- `fcntl` を使ったソケットのノンブロッキング化（O_NONBLOCK）。
- `poll()` を中心としたメインループ（`while(1)`）の構築。
- イベント駆動による `accept`, `recv`, `send` の安全な呼び出しと、切断検知時の `close` 処理。

**【独立テストの方法】**
ConfigやHTTPパーサーは不要。「ポート8080で起動し、ブラウザからアクセスが来たら固定の "Hello World" レスポンス文字列を `send` する」という状態まで単独で作り上げる。

---

## 開発・ブランチ

1. **共有ヘッダー作成:** 全員で `main` ブランチにインターフェースとなる `.hpp` を作成しコミット。
2. **ブランチ作成:** 各自が `main` からブランチを切る。
3. **独立開発:** 他の担当の進捗を待つことなく、モック（仮のデータ）を使って自身のコンポーネントを完成させる。
4. **統合テスト:** 3つのブランチが形になったら `main` にマージし、「設定ファイルを読み込み、ポートを開き、ブラウザに正しいページを返す」フルフローをテストする。

**※注意:** CGIの実装（`fork`, `execve`, `pipe`, `dup2`）は、この基礎サーバーが完全に動作してから着手する。

## コーディングルール
1. **42ヘッダー禁止:** ファイルが長くなるため。
2. **インデントはタブ文字**
3. **.tpp, .ippファイル使用不可:** ファイル数が多くなり、ファイルをまたがる検索や修正に負荷がかかるため。
